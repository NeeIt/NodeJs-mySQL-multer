<!DOCTYPE html>
<html>

<head>
    <title>Список пользователей</title>
    <meta charset="utf-8" />
    <style>
        * {
            margin: 0px;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
            padding: 0px;
        }

        img {
            height: 100px;
            width: 100px;
            object-fit: cover;
        }

        tr:first-of-type {
            background-color: #EFF7FF;
        }

        td {
            padding: 10px;
            text-align: center;
        }

        .wrapperTable {
            background-color: #EFF7F7;
            margin-top: 20px;
        }

        table {
            width: 100%;
            background-color: #fff;
            border: 2px solid #DEDEE6;
        }

        .avatar {
            width: 100px;
        }

        .panel {
            width: 150px;
        }

        h1 {
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #21294A;
            color: white;
            font-size: 1.4em;

        }

        body {
            background-color: #EFEFEF;
        }

        .body {
            padding: 50px;
        }

        .add {
            color: white;
            background-color: limegreen;
            border-radius: 20px;
            padding: 10px 50px;
            width: 70px;
            text-align: center;
        }

        a {
            text-decoration: none;
        }
    </style>
</head>

<body>
    <h1>Список пользователей</h1>
    <div class="body">
        <a href="/create">
            <div class="add">Добавить</div>
        </a>
        <div class="wrapperTable">
            <table>
                <tr>
                    <th>Аватар</th>
                    <th>Имя</th>
                    <th>Возраст</th>
                    <th></th>
                </tr>
                {{#each users}}
                <tr>
                    <td class="avatar"><img src="/image/{{this.image}}" /></td>
                    <td>{{this.name}}</td>
                    <td>{{this.age}}</td>
                    <td class="panel">
                        <a href="/edit/{{this.id}}"><button>Ред</button></a>
                        <form action="delete/{{this.id}}" method="POST" style="display:inline;">
                            <input type="submit" value="Удалить" />
                        </form>
                    </td>
                </tr>
                {{/each}}
            </table>
        </div>
    </div>

</body>
<html>